# buf.yaml - Buf configuration for protocol buffer management
version: v2
modules:
  - path: proto
    name: buf.build/opencode-ai/swarm
lint:
  use:
    - STANDARD
  except:
    - PACKAGE_VERSION_SUFFIX
breaking:
  use:
    - FILE
